[package]
name = "gui"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14.0", features = ["tokio"] }
client = { path = "../client" }
crypto = { path = "../crypto" }
hex = "0.4.3"
tokio = { version = "1", features = ["sync"] }
